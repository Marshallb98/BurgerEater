<h1>Burgies!</h1>

<h2>Burgers that NEED to be eaten!</h2>

<ul>
    {{#each burgers}}
    {{#unless devoured}}
    {{> burgers/burger-block devoured=true}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that have been slain</h2>

<ul>
    {{#each burgers}}
    {{#if devoured}}
    {{> burgers/burger-block devoured=false}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Burgie</h2>
<form class="create-burger">

    <div class="form-group">
        <label for="burgerName">Burgies Name</label>
        <input type="text" id="burgerName" name="burger_name">
    </div>

    <div class="form-group">
        <input type="radio" name="devoured" value="0"> NEEDS to be eaten!<br>
        <input type="radio" name="devoured" value="1"> Slain!
    </div>

    <button type="submit">Add this Burgie to my collection!</button>
</form>